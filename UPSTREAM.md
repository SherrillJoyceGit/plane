# Plane 上游同步记录

## 当前基线

- 上游项目：Plane
- 上游标签：`v1.4.2`
- 上游提交：`5f7d92784c403f76284f0f16718f320221dc7fec`

## 已引入的上游修复

暂无。

## 同步约定

- 单个修复使用 `git cherry-pick -x <commit>` 引入，并在本节记录原始提交。
- 大版本同步使用 `sync/plane-vX.Y.Z` 临时分支，验证后合入 `main`。
- 已发布的 `main` 历史不得改写。
